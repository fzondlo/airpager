<div class="max-w-5xl mx-auto py-8 px-6">
  <div class="flex items-center justify-between mb-6">
    <h1 class="text-2xl font-bold text-gray-800">Properties</h1>
  </div>

  <p class="text-sm text-gray-600 my-4">
    This is a non-public view intended for the Airpager development team. It displays all properties currently stored in the database. Use it to inspect data, verify integration syncs, and monitor property metadata.
  </p>

  <% if @missing_hospitable_properties.any? %>
    <div class="bg-yellow-50 border border-yellow-300 text-yellow-800 text-sm rounded-lg p-4 mb-6">
      <strong class="font-medium">⚠️ Missing Hospitable Properties</strong>
      <p class="text-xs mt-1 text-yellow-700">This list is cached and refreshes every 5 minutes.</p>
      <ul class="mt-2 list-disc list-inside">
        <% @missing_hospitable_properties.each do |property| %>
          <li>
            <span class="font-semibold"><%= property["name"] %></span> — ID: <%= property["id"] %>
          </li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="overflow-x-auto bg-white shadow-md rounded-xl">
    <table class="min-w-full table-auto text-sm text-left text-gray-700">
      <thead class="bg-gray-100 text-gray-600 uppercase text-xs py-2">
        <tr>
          <th class="px-6 py-3">ID</th>
          <th class="px-6 py-3">Name</th>
          <th class="px-6 py-3">Clickup custom field ID</th>
          <th class="px-6 py-3">Hospitable ID</th>
          <th class="px-6 py-3 text-right">Actions</th>
        </tr>
      </thead>
      <tbody>
        <% @properties.each do |property| %>
          <tr class="border-t border-gray-200 hover:bg-gray-50">
            <td class="px-6 py-4 text-gray-600 whitespace-nowrap">
              <%= property.id %>
            </td>
            <td class="px-6 py-4 font-bold">
              <%= property.name %>
            </td>
            <td class="px-6 py-4">
              <%= property.clickup_custom_field_id %>
            </td>
            <td class="px-6 py-4">
              <%= property.hospitable_id %>
            </td>
            <td class="px-6 py-4 text-right space-x-2">
              <div class="inline-flex items-center gap-2 text-gray-600">
                <%= link_to "Show", property_path(property.id), class: "text-blue-600 hover:underline" %>
              </div>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
